#include "Analyzer.h"

int Analyzer::init(const Params &params) {
    return 0;
}

int Analyzer::analyze(const FrameInfo &frameInfo, RoadInfo &roadInfo) const {
    return 0;
}

void Analyzer::release() {}
